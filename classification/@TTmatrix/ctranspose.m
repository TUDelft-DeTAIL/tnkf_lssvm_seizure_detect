function [At] = ctranspose(A)
%ctranspose(A) computes the transpose of the TTmatrix A. The ' operator can
%   be use instead of transpose(A).
%
%INPUT:
%   A : a TTmatrix object
%OUTPUT
%   At : the transpose of A


At = transpose(A);